apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: disk-exporter
  namespace: prometheus
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: disk-exporter
  endpoints:
  - port: metrics
    path: /metrics
    interval: 60s

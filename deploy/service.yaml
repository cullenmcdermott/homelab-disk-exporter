apiVersion: v1
kind: Service
metadata:
  name: disk-exporter
  namespace: prometheus
  labels:
    app.kubernetes.io/name: disk-exporter
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: disk-exporter
  ports:
  - name: metrics
    port: 9999
    targetPort: metrics
    protocol: TCP
